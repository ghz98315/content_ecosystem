# 历史类流程最新进度

更新时间：2026-08-24

## 当前状态

- 历史类 book 阶段不再调用 LLM 反推书名或生成 CTA，只读取 rewrite 中明确提供的书名和原文 CTA。
- 历史类改写规则已加强：开头 3-4 句保留，中段实质重构，CTA 原文保留，不猜书名，保留史实和不确定性，短句与 10-12 字口播停顿；合规失败自动再生成一轮。
- 历史类图片按语义事件和段落规划，每分钟最多 3 张，不是整篇最多 3 张。
- 历史类采用竖版 `1024x1792` 九宫格生图，后台切成独立 9:16 图片；健康类仍使用原有横版九宫格与 4:3 切割。
- 历史类正文不显示书名、作者和底部免责；封面前 15 帧显示用户确认的主标题、副标题，居中、黄色楷体优先、黑色细描边。
- 主标题和副标题在改写确认页填写并保存，生产设置继续只管理视觉样式和生图参数。

## 已推送提交

- `bb6c3a0`：历史类封面主副标题前置确认并接入渲染。
- `8f2150b`：历史类九宫格竖版生图和 9:16 切割。
- `e291726`、`0e6c53a`：历史类改写合规检查和失败自动重试。

## 验证结论

- Worker `compileall` 通过（`worker/.pytest_cache` 提示可忽略）。
- Web `npm.cmd run build` 通过。
- 旧测试中有两项断言对应旧规则：整篇最多 3 张、相似度 0.83 通过；后续需按新规则更新测试。

## 重启后的下一步

1. 新建历史类任务，完整运行 `rewrite -> image -> book -> tts -> render`。
2. 在改写确认页填写主标题和副标题并确认。
3. 检查 `history_director_analysis`、`image_grid`、独立图片比例和封面前 15 帧。
4. 旧任务只有从 rewrite 阶段重跑，才能完整验证新逻辑；只重跑 render 只能验证画面。
