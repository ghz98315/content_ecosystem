# 任务音色样本试听 checkpoint

更新时间：2026-08-12

## 已完成

- 视频采集工作台的任务音色选择现在读取已启用音色档案的样本路径。
- 选择有样本的音色后，通过已有签名 URL 接口加载并展示音频试听控件。
- 系统默认 Edge 明确说明为 Worker 兜底音色；没有已登记样本。
- 缺少样本或签名 URL 失败时展示对应状态，不影响任务创建或任务级快照。

## 验证

- `npm.cmd run build`（`web`）：通过。
- `git diff --check`：通过，仅有既有换行格式提示。

## 线上检查

部署后在 `/video-collection` 选择 CosyVoice 音色档案，确认播放器可加载授权样本；再创建一条新的健康类测试任务。
