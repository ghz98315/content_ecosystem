# P3 媒体生产人工检查检查点

日期：2026-08-12

## 本次完成

- 图片工作台新增全部、已加载、待加载状态筛选。
- 图片卡片新增单图检查抽屉，展示真实图片、镜头文案、预计时长、字数、动效和来源批次。
- 成片页读取 Worker 已生成的 `render_timeline`，展示镜头序号、字幕文案、起止时间和持续时长。
- 未添加没有后端支持的单图重生成、Provider 切换或历史成片修改按钮。

## 验证

- `npm.cmd run build`（`web/`）：通过。
- `python -m compileall -q .`（`worker/`）：通过。
- `git diff --check`：通过。
- 未启动全局 Worker，也未执行真实付费生图或成片任务。
- 本机 Node 为 v22.19.0，项目期望 Node 20.x。

## 剩余 P3

- 镜头级重生成、违规替换原因和版本记录需要新增后端接口与迁移。
- TTS Provider/Voice ID 快照、试听与字幕导出仍待后续切片。
- 成片风格、背景音乐、输出数量配置仍需 Worker 参数化后开放。
