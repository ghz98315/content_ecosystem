第 3 步：TTS 合成音频 + 反推书籍信息——用 index-tts2 语音克隆，语音克隆这块建议找现成的 api 接口处理，我目前用下来 index-tts2 比较靠谱，千问、minimax 也用过但感觉人物情感还是差点意思，如果大家没有显卡也建议调用三方 API，我自己在腾讯云部署过index-tts2模型，生成速度很慢，且综合下来成本也不便宜，还有个问题是一般的配音模型会有上下限制，例如 限制单次提交1000 字以内，此时可以进行切分再提交，最后将多段音频在本地合成。（具体的提示词在文章底部有提供）


停顿控制：​
，（逗号）停顿0.3 秒​
。（句号）停顿0.8 秒​
！？（感叹 / 问号）停顿0.8-1.0 秒​
——（破折号）停顿0.6-0.8 秒​
……（省略号）停顿0.8-1.0 秒​
；：（分号 / 冒号）停顿0.4-0.5 秒​
“ ”（双引号）前后各停顿 0.1-0.2 秒

TTS 段落切分（让长文案适配 TTS 接口）​
很多 TTS 接口对单次输入有时长限制（一般 24-30 秒）。需要把长文案切成多段后并发合成。​
System：​
​
代码块​
你是中文短视频配音文案拆段助手。​
你的唯一任务是把给定文案按原顺序拆成多个适合 TTS 的自然段。​
严禁改写、增删、概括、润色或重排内容。​
输出必须是严格 JSON：{"segments": ["...", "..."]}。​
每段尽量控制在 24 到 28 秒内，绝不能故意合并成长段。​
如果原文本来很短，也至少返回一个 segment。​
不要输出 markdown，不要输出解释。​
​
User：​
​
代码块​
主题关键词：{keyword}​
原视频标题：{title}​
原作者标识：{author}​
目标单段时长：26 秒以内​
​
请基于下面这段候选稿（最终配音文案）拆段：​
{script_text}​
​
关键经验：切分后并发合成可以把 90 秒文案的 TTS 时间从 25 秒压到 5 秒以内。如果你不做并发，用单接口顺序请求也可以。