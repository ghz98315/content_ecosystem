# M0 骨架 + M1 采集依赖
supabase>=2.4.0
python-dotenv>=1.0.0

# HTTP / SSL（supabase-py 底层；certifi 修复 Windows SSL EOF 问题）
httpx>=0.28.0
certifi>=2026.0.0

# 大模型调用
openai>=1.30.0

# ASR 逐字稿（本地 CPU/GPU 推理）
faster-whisper>=1.0.0

# 抖音自研解析（f2 内置 a_bogus 签名）
f2==0.0.1.7
yt-dlp>=2026.6.9              # 备用/兜底解析

# 版本约束：f2 会把这两个降级，需固定到 supabase realtime 可用的范围
pydantic>=2.11.7
websockets>=13,<16

# 音频抽取（自带静态 ffmpeg 二进制，免系统安装；M2 whisper / M8 渲染复用）
imageio-ffmpeg>=0.5.1

# TTS 配音（句级时间戳，edge-tts 7.x 中文只给 SentenceBoundary）
edge-tts>=7.2.0
# 字幕中文分词（保护词语完整性，避免按固定字数硬切）
jieba>=0.42.1

# 生图：9宫格切割
Pillow>=10.0.0
